# Ansible/local
*.retry
.env
.env.*
secrets.yml
vault.yml

# Internal audit and working documents
docs/project-accuracy-audit-*.md

# Credentials — keep local overrides out of the repo
ansible/inventory.local.ini
group_vars/local_vault.yml

