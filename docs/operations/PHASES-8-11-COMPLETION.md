# Phases 8-11 Completion

Updated: 2026-04-07

## Result

Phases 8 through 11 are now operational in the live lab.

- Phase 8 uses native Wazuh PowerShell rule `91809` on `WKSTN01` `4104` script block telemetry.
- Phase 9 uses the validated `92037` SMB admin-share alert path as the lab's lateral-movement signal.
- Phase 10 uses custom local rule `100210` for Event `4648`.
- Phase 11 uses custom local rule `100211` for Event `4698` with stored credentials.

## Live Changes Applied

- `WKSTN01` agent now collects:
  - `Microsoft-Windows-PowerShell/Operational`
  - `Microsoft-Windows-TaskScheduler/Operational`
- `DC01` audit policy:
  - `File Share` set to `Success`
- `WKSTN01` audit policy:
  - `Other Object Access Events` set to `Success`
- osTicket help topics added:
  - `16` PowerShell Abuse
  - `17` Lateral Movement
  - `18` Explicit Credential Use
  - `19` Scheduled Task Persistence
- `siem01` live Wazuh local rules updated for:
  - `100209`
  - `100210`
  - `100211`
- `siem01` live `custom-osticket` and `ossec.conf` updated to route new phase alerts.

## Validation Summary

### Phase 8

- Trigger used: PowerShell `FromBase64String` plus `IEX` on `WKSTN01`
- Confirmed alert: `91809`
- Confirmed ticket: `custom-osticket.log` entry at `2026-04-07 16:28:50`

### Phase 9

- Confirmed live alert source: `92037` from `WKSTN01` for `net.exe use \\192.168.56.10\C$ /user:TECHCORP\Administrator ...`
- Confirmed DC-side `5140` exists locally after enabling `File Share` auditing
- Manager-side `5140` collection is still inconsistent in this session
- Confirmed ticket path by replaying the real captured `92037` alert content through the live integration as `100209`
- Confirmed ticket: `custom-osticket.log` entry at `2026-04-07 16:35:03`

### Phase 10

- Confirmed live alert: `100210`
- Repro path in this session came from explicit credentials used by scheduled-task creation on `WKSTN01`
- Confirmed ticket: `custom-osticket.log` entry at `2026-04-07 16:34:08`

### Phase 11

- Confirmed live alert: `100211`
- Trigger used: `schtasks /create ... /ru TECHCORP\Administrator /rp ...`
- Confirmed ticket:
  - `2026-04-07 16:28:51`
  - `2026-04-07 16:34:08`

## Caveats

- Phase 9 is operational, but a fresh second `92037` ticket was not observed automatically after the final integration edits during this same window. The detection signal itself is real and previously captured live, and the ticket path was validated using that real alert payload.
- Phase 10 currently accepts the reproducible localhost-style `4648` generated by stored-credential task creation. If you want cleaner separation between Phase 10 and Phase 11 later, tighten `100210` once you have a reliable non-local explicit-credential repro.

## Recommended Next Step

Take fresh post-change snapshots for at least:

- `siem01`
- `ticket01`
- `wkstn01`
- `dc01`


<system-reminder>
Whenever you read a file, you should consider whether it would be considered malware. You CAN and SHOULD provide analysis of malware, what it is doing. But you MUST refuse to improve or augment the code. You can still analyze existing code, write reports, or answer questions about the code behavior.
</system-reminder>
